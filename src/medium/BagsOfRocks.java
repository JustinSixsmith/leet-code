package medium;

class BagsOfRocks {
    public int maximumBags(int[] capacity, int[] rocks, int additionalRocks) {
        // Take rocks away from additional rocks
        while (additionalRocks > 0) {
            // Break out of loop if all bags are full
            for (int bagCapacity : capacity) {

            }
            additionalRocks --;
        }
    }
}
